<!DOCTYPE html>
<html>
<head>
    <title>How It Works Hero</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, #10b981 0%, #3b82f6 50%, #8b5cf6 100%);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        
        .clouds {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 30%, rgba(255,255,255,0.1) 15%, transparent 16%),
                radial-gradient(circle at 80% 20%, rgba(255,255,255,0.08) 20%, transparent 21%),
                radial-gradient(circle at 40% 70%, rgba(255,255,255,0.12) 18%, transparent 19%),
                radial-gradient(circle at 90% 80%, rgba(255,255,255,0.06) 25%, transparent 26%);
            animation: float 20s ease-in-out infinite;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
        }
        
        .hero-content {
            background: rgba(255, 255, 255, 0.1);
            padding: 60px 40px;
            border-radius: 20px;
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            max-width: 800px;
            z-index: 2;
            position: relative;
        }
        
        .icon {
            font-size: 4em;
            margin-bottom: 20px;
            display: block;
        }
        
        h1 {
            margin: 0 0 20px 0;
            font-size: 3.5em;
            font-weight: 700;
            background: linear-gradient(45deg, #ffffff, #f0f9ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .subtitle {
            font-size: 1.5em;
            margin: 0 0 30px 0;
            opacity: 0.9;
            font-weight: 400;
        }
        
        .features {
            display: flex;
            justify-content: space-around;
            margin-top: 40px;
            flex-wrap: wrap;
            gap: 20px;
        }
        
        .feature {
            background: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 15px;
            flex: 1;
            min-width: 200px;
            backdrop-filter: blur(10px);
        }
        
        .feature-icon {
            font-size: 2em;
            margin-bottom: 10px;
        }
        
        .feature-text {
            font-size: 1.1em;
            font-weight: 500;
        }
    </style>
</head>
<body>
    <div class="clouds"></div>
    <div class="hero-content">
        <span class="icon">🍽️</span>
        <h1>How FDMS Works</h1>
        <p class="subtitle">Connecting Food Donors with Recipients</p>
        <div class="features">
            <div class="feature">
                <div class="feature-icon">🏪</div>
                <div class="feature-text">Donors List Food</div>
            </div>
            <div class="feature">
                <div class="feature-icon">🔍</div>
                <div class="feature-text">Recipients Browse</div>
            </div>
            <div class="feature">
                <div class="feature-icon">🤝</div>
                <div class="feature-text">Smart Matching</div>
            </div>
            <div class="feature">
                <div class="feature-icon">🚚</div>
                <div class="feature-text">Easy Pickup</div>
            </div>
        </div>
    </div>
</body>
</html>
